단건 상품 쿠폰 신규 도입 & 구독 쿠폰 변경사항
작성일 2026-08-04
대상: 어드민 운영팀
작성: 개발팀
한눈에 보기
NEW 그동안 구독에만 있던 할인 쿠폰이 단건 상품 주문에도 생겼습니다. 어드민에서 별도로 발급·관리합니다.
CHANGED 구독 쿠폰의 같은 코드 재사용 방지가 지금까지 실제로는 동작하지 않던 버그였습니다. 이번에 고쳐서, 이제 한 고객이 같은 쿠폰 코드를 다시 쓰면 막힙니다.
둘 다 쿠폰 생성·수정은 슈퍼 관리자(Admin) 권한 계정에서만 가능합니다. 가게 사장님(StoreOwner) 계정에는 노출되지 않습니다.
NEW
단건 상품 쿠폰
주문 화면에서 고객이 쿠폰 코드를 입력하면, 결제 금액에서 할인이 빠진 채로 결제가 진행됩니다.

구독 쿠폰과 무엇이 다른가
구분 구독 쿠폰 단건 상품 쿠폰
할인 적용 범위 단가 1개분에만 적용 (수량 늘려도 할인액 동일) 주문 총액(단가 × 수량) 전체에 적용
최대 할인 금액 캡 없음 있음 — 쿠폰마다 maxDiscountAmount로 상한 설정 가능
적용 시점 최초 결제(구독 시작) 시 1회. 매달 자동 갱신 결제에는 재적용 안 됨 주문 1건 결제 시. 단건은 원래 1회성 결제라 매달 개념 자체가 없음
같은 코드 재사용 불가 (이번에 수정) — 아래 변경사항 참고 불가 — 처음부터 이렇게 구현됨
어드민 관리 화면 /admin/coupons /admin/product-coupons (별도 테이블, 별도 관리)
어드민 API
구독 쿠폰 관리 화면과 동일한 방식이며, 엔드포인트만 다릅니다.

GET
/admin/product-coupons?page=1&limit=20
— 쿠폰 목록 조회
POST
/admin/product-coupons
— 쿠폰 생성
PATCH
/admin/product-coupons/:id
— 쿠폰 수정 (활성/비활성 전환 포함)
쿠폰 필드
필드 필수 설명
code 생성 시 필수 쿠폰 코드, 최대 30자. 대소문자 구분 없이 저장·조회됨
name 선택 쿠폰 이름 (고객에게 노출)
description 선택 쿠폰 설명
discountRate 생성 시 필수 할인율, 1~100 (%)
maxDiscountAmount 선택 최대 할인 금액(원). 비워두면 할인액 상한 없음
isActive 수정 전용 비활성화하면 즉시 사용 불가 처리됨 (생성 시 항상 활성 상태로 시작)
startDate / endDate 선택 사용 가능 기간. 비워두면 기간 제한 없음
생성 예시
POST /admin/product-coupons

{
"code": "summer20",
"name": "여름맞이 20%",
"discountRate": 20,
"maxDiscountAmount": 10000,
"startDate": "2026-08-01",
"endDate": "2026-08-31"
}
→ 15,000원짜리 상품을 3개(45,000원) 주문해도 할인은 45,000원의 20%인 9,000원이 아니라, maxDiscountAmount 상한인 최대 10,000원까지만 적용됩니다.

캡 없는 쿠폰 예
discountRate: 10, maxDiscountAmount: null

50,000원 주문 → 5,000원 할인

캡 있는 쿠폰 예
discountRate: 30, maxDiscountAmount: 15000

100,000원 주문 → 30% 할인은 30,000원이지만 상한이 있어 15,000원만 할인

CHANGED
구독 쿠폰 — 재사용 방지 버그 수정
이전 상태: "같은 쿠폰을 같은 고객이 두 번 못 쓰게 막는다"는 로직이 코드에 준비만 되어 있고, 실제로는 어디에서도 호출되지 않고 있었습니다. 그래서 지금까지는 고객이 같은 쿠폰 코드를 여러 번 등록해도 매번 할인이 적용됐습니다.

수정 후: 구독 생성 시, 그리고 결제 전 쿠폰 미리보기 조회 시 모두 "이 고객이 이 쿠폰을 이미 쓴 적 있는지"를 확인합니다. 이미 쓴 적이 있다면 이미 사용한 쿠폰입니다 오류로 막힙니다.

제한 단위는 "쿠폰 코드당"입니다. 한 고객이 쿠폰 A를 한 번 쓰고 나면 쿠폰 A는 다시 못 쓰지만, 쿠폰 B는 처음이라면 그대로 쓸 수 있습니다. 여러 고객이 같은 쿠폰 코드를 나눠 쓰는 것도 그대로 가능합니다 — 막히는 건 "같은 고객 + 같은 쿠폰"의 조합뿐입니다.
바뀌지 않은 것
이번 수정은 재사용 방지 하나만 고친 것이고, 아래 기존 정책은 그대로입니다.

항목 기존 정책 (유지됨)
할인 적용 범위 수량과 무관하게 단가 1개분에만 할인 적용
적용 시점 구독 최초 결제 시 1회만. 매달 자동 갱신 결제에는 할인이 다시 적용되지 않고 정가로 청구됨
어드민 화면 /admin/coupons 그대로, API/필드 변경 없음
과거에 이미 여러 번 쓰인 쿠폰이 있다면, 그 이력은 그대로 남고 이번 수정으로 소급 제재되지 않습니다. 이번 배포 이후 시점부터 재사용이 막힙니다.
운영 시 참고사항
항목 내용
쿠폰 코드 대소문자 구독·단건 모두 구분하지 않습니다. SUMMER20과 summer20은 같은 쿠폰으로 취급됩니다.
기간 만료 자동 처리 없습니다. endDate가 지나면 고객에게는 사용 불가로 보이지만, 목록에서 자동으로 비활성 처리되지는 않습니다. 정리가 필요하면 isActive를 수동으로 꺼주세요.
쿠폰별 사용 내역 조회 현재 어드민 화면에는 없습니다. 필요하시면 개발팀에 요청해주세요.
접근 권한 구독·단건 쿠폰 관리 API 모두 슈퍼 관리자(Admin) 권한 계정만 접근 가능합니다.
